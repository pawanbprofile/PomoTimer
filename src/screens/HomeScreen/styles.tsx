import {StyleSheet} from 'react-native';
import Colors from '../../utils/Colors';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: Colors.orangeTheme.primaryColor,
    justifyContent: 'center',
    alignItems: 'center',
  },
});

export default styles;
